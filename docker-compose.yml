version: '3.9'

services:
    
    docker-react:
        container_name: sdlc-rrdc
        build: 
            context: .
            dockerfile: Dockerfile
        volumes:
            - '.:/app'
            - '/app/node_modules'
        ports:
            - 3001:3000
        environment:
        - CHOKIDAR_USEPOLLING=true